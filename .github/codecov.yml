coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
ignore:
  # Directories that contain only tests and test data.
  - "**/benchmark/**"
  - "**/testdata/**"
  # Helpers that only tests use.
  - "internal/testhelp"
comment:
  layout: "header, diff, files"
